<li>
  <%= gravatar_for user, size: 30 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
  | 
  <a href="/users/<%= user.id %>/delete" onclick="if(confirm('Are you sure you want to delete this page?'))  return true;  else  return false;" >Destroy
  <% end %>
</li>